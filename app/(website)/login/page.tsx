export const metadata = {
  title: "Login",
  description: "This is the login page",
};
import LoginPage from "./components/Login";

const page = () => {
  return <LoginPage />;
};

export default page;
